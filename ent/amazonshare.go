// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/PaluMacil/dan2/ent/amazonshare"
)

// AmazonShare is the model entity for the AmazonShare schema.
type AmazonShare struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// CanEdit holds the value of the "can_edit" field.
	CanEdit bool `json:"can_edit,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the AmazonShareQuery when eager-loading is set.
	Edges        AmazonShareEdges `json:"edges"`
	selectValues sql.SelectValues
}

// AmazonShareEdges holds the relations/edges for other nodes in the graph.
type AmazonShareEdges struct {
	// User holds the value of the user edge.
	User []*User `json:"user,omitempty"`
	// AmazonList holds the value of the amazon_list edge.
	AmazonList []*AmazonList `json:"amazon_list,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// UserOrErr returns the User value or an error if the edge
// was not loaded in eager-loading.
func (e AmazonShareEdges) UserOrErr() ([]*User, error) {
	if e.loadedTypes[0] {
		return e.User, nil
	}
	return nil, &NotLoadedError{edge: "user"}
}

// AmazonListOrErr returns the AmazonList value or an error if the edge
// was not loaded in eager-loading.
func (e AmazonShareEdges) AmazonListOrErr() ([]*AmazonList, error) {
	if e.loadedTypes[1] {
		return e.AmazonList, nil
	}
	return nil, &NotLoadedError{edge: "amazon_list"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*AmazonShare) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case amazonshare.FieldCanEdit:
			values[i] = new(sql.NullBool)
		case amazonshare.FieldID:
			values[i] = new(sql.NullInt64)
		case amazonshare.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the AmazonShare fields.
func (as *AmazonShare) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case amazonshare.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			as.ID = int(value.Int64)
		case amazonshare.FieldCanEdit:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field can_edit", values[i])
			} else if value.Valid {
				as.CanEdit = value.Bool
			}
		case amazonshare.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				as.CreatedAt = value.Time
			}
		default:
			as.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the AmazonShare.
// This includes values selected through modifiers, order, etc.
func (as *AmazonShare) Value(name string) (ent.Value, error) {
	return as.selectValues.Get(name)
}

// QueryUser queries the "user" edge of the AmazonShare entity.
func (as *AmazonShare) QueryUser() *UserQuery {
	return NewAmazonShareClient(as.config).QueryUser(as)
}

// QueryAmazonList queries the "amazon_list" edge of the AmazonShare entity.
func (as *AmazonShare) QueryAmazonList() *AmazonListQuery {
	return NewAmazonShareClient(as.config).QueryAmazonList(as)
}

// Update returns a builder for updating this AmazonShare.
// Note that you need to call AmazonShare.Unwrap() before calling this method if this AmazonShare
// was returned from a transaction, and the transaction was committed or rolled back.
func (as *AmazonShare) Update() *AmazonShareUpdateOne {
	return NewAmazonShareClient(as.config).UpdateOne(as)
}

// Unwrap unwraps the AmazonShare entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (as *AmazonShare) Unwrap() *AmazonShare {
	_tx, ok := as.config.driver.(*txDriver)
	if !ok {
		panic("ent: AmazonShare is not a transactional entity")
	}
	as.config.driver = _tx.drv
	return as
}

// String implements the fmt.Stringer.
func (as *AmazonShare) String() string {
	var builder strings.Builder
	builder.WriteString("AmazonShare(")
	builder.WriteString(fmt.Sprintf("id=%v, ", as.ID))
	builder.WriteString("can_edit=")
	builder.WriteString(fmt.Sprintf("%v", as.CanEdit))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(as.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// AmazonShares is a parsable slice of AmazonShare.
type AmazonShares []*AmazonShare
